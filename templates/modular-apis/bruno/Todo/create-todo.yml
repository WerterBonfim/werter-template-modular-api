info:
  name: create-todo
  type: http
  seq: 4

http:
  method: POST
  url: "{{todo}}/todos"
  body:
    type: json
    data: |-
      {
        "name": "Write tests",
        "isComplete": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​