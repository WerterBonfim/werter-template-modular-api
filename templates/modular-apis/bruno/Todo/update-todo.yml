info:
  name: update-todo
  type: http
  seq: 5

http:
  method: PUT
  url: "{{todo}}/todos/:id"
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "Write tests",
        "isComplete": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
